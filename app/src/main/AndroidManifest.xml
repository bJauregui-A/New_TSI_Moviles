<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  xmlns:tools="http://schemas.android.com/tools">
	<uses-permission android:name="android.permission.INTERNET"/>

	<application
			android:usesCleartextTraffic="true"
			android:allowBackup="true"
			android:dataExtractionRules="@xml/data_extraction_rules"
			android:fullBackupContent="@xml/backup_rules"
			android:label="@string/app_name"
			android:supportsRtl="true"
			android:theme="@style/Theme.New_TSI_Moviles">

		<activity
				android:name=".MainActivity"

				android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".controller.Principal"/>
		<activity android:name=".controller.CursoController"/>
		<activity android:name=".controller.CursoUserController"/>
		<activity android:name=".controller.VistaCurso"/>
		<activity android:name=".controller.PerfilController"/>
		<activity android:name=".controller.VistaCursoUser"/>
		<activity android:name=".controller.CrearCursoController"/>
		<activity android:name=".controller.EditCurso"/>
		<activity android:name=".controller.CertificadosController"/>
		<activity android:name=".controller.BadJokeController"/>
		<activity android:name=".controller.UsuarioController"/>
		<activity android:name=".controller.EditUserController"/>
		<activity android:name=".controller.CreateUserController"/>
		<activity android:name=".controller.MapaController"/>

	</application>

</manifest>